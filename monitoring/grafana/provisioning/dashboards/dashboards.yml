apiVersion: 1

# Dashboards são importados via API (não provisionados por arquivo)
# para permitir edição livre pelo UI do Grafana.
# Backup: monitoring/grafana/banking-simulation.json.bak
providers: []
