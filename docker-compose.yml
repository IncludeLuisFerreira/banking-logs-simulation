services:
  app:
    build: ./app
    container_name: banking-sistem
    ports:
      - "3000:3000"
    volumes:
      - ./app/logs:/app/logs
      - ./app/public:/app/public
    restart: unless-stopped